// Revise bitwise operator 

class Solution {
    public boolean consecutiveSetBits(int n) {
        int x = n & (n >> 1);

        if(Integer.bitCount(x)==1) return true;
        return false;
    }
}
